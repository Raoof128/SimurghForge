# ESLint 10 + TypeScript 6 exceed current peer ranges for typescript-eslint / eslint-plugin-react-hooks.
# Keeps npm ci reproducible on GitHub Actions (same tree as local installs with --legacy-peer-deps).
legacy-peer-deps=true
